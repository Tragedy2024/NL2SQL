# SSA for database: financial
annotator: GPT-4o + human_review
# Auto-generated by GPT-4o, reviewed by human
# Generated: 2026-07-14
#
column_labels:
  account:
    account_id: free  # identifier
    district_id: free  # location ID (foreign key)
    frequency: free  # category label
    date: free  # date-only
  card:
    card_id: free  # identifier
    disp_id: free  # foreign key
    type: free  # category label
    issued: free  # date-only
  client:
    client_id: free  # identifier
    gender: free  # category label
    birth_date: free  # date-only, no personal name linkage
    district_id: free  # location ID (foreign key)
  disp:
    disp_id: free  # identifier
    client_id: free  # foreign key
    account_id: free  # foreign key
    type: free  # category label
  district:
    district_id: free  # identifier
    A2: free  # district name (location)
    A3: free  # region name (location)
    A4: free  # aggregate statistic
    A5: free  # aggregate statistic
    A6: free  # aggregate statistic
    A7: free  # aggregate statistic
    A8: free  # aggregate statistic
    A9: free  # aggregate statistic (unknown, assumed public)
    A10: free  # aggregate statistic
    A11: controlled  # financial figure (average salary)
    A12: free  # aggregate statistic
    A13: free  # aggregate statistic
    A14: free  # aggregate statistic
    A15: free  # aggregate statistic
    A16: free  # aggregate statistic
  loan:
    loan_id: free  # identifier
    account_id: free  # foreign key
    date: free  # date-only
    amount: controlled  # financial amount
    duration: free  # numeric duration, non‑personal
    payments: controlled  # financial amount
    status: free  # category label
  order:
    order_id: free  # identifier
    account_id: free  # foreign key
    bank_to: free  # bank name
    account_to: controlled  # financial account identifier
    amount: controlled  # financial amount
    k_symbol: free  # category label
  trans:
    trans_id: free  # identifier
    account_id: free  # foreign key
    date: free  # date-only
    type: free  # category label
    operation: free  # category label
    amount: controlled  # financial amount
    balance: controlled  # financial amount (balance)
    k_symbol: free  # category label
    bank: free  # bank name
    account: controlled  # financial account identifier