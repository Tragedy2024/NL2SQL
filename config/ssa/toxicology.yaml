# SSA for database: toxicology
annotator: GPT-4o + human_review
# Auto-generated by GPT-4o, reviewed by human
# Generated: 2026-07-14
#
column_labels:
  atom:
    atom_id: free  # primary key, synthetic ID
    molecule_id: free  # foreign key ID
    element: free  # chemical element category
  bond:
    bond_id: free  # primary key
    molecule_id: free  # foreign key
    bond_type: free  # bond type category
  connected:
    atom_id: free  # composite primary key, foreign key
    atom_id2: free  # composite primary key, foreign key
    bond_id: free  # foreign key
  molecule:
    molecule_id: free  # primary key
    label: free  # categorical label (carcinogenic classification)